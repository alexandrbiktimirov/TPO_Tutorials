spring:
  banner:
    location: logo-app
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:file:./data/data
    username: sa
    password:
  sql:
    init:
      mode: always